<script>
    var selected_targets = [<%= raw @selected_target.to_json %>];
    var reference_buyers = <%= raw @reference_buyers.to_json %>;
    var selected_buyers = [<%= raw @selected_buyer.to_json %>];
    var fixed_target_attrs = <%= raw APP_CONFIG['target_fixed_attrs'].to_json %>;
    var target_attrs = <%= raw APP_CONFIG['target_attrs'].select {|k, v| @target_attrs.include?(k) }.to_json %>;
    var fixed_buyer_attrs = <%= raw APP_CONFIG['buyer_fixed_attrs'].to_json %>;
    var buyer_attrs = <%= raw APP_CONFIG['buyer_attrs'].select { |k, v| @buyer_attrs.include?(k) }.to_json %>;
</script>

<%= render "flow" %>
<h1>行业: <%= @industry.name %></h1>
<h2>参考的买家</h2>
<div id="reference-buyer-grid">
  <table id="reference-buyer-grid-table"></table>
  <div class="pager"></div>
</div>
<h2>选择的买家</h2>
<div id="selected-buyer-grid">
  <table id="selected-buyer-grid-table"></table>
  <div class="pager"></div>
</div>
<h2>选择的目标</h2>
<div id="selected-target-grid">
  <table id="selected-target-grid-table"></table>
  <div class="pager"></div>
</div>

<p>
  <%= link_to "重新选择", total_select_industry_path, class: 'btn btn-primary' %>
</p>