<script>
var buyers = <%= raw @buyers.to_json %>;
var fixed_attrs = <%= raw APP_CONFIG['buyer_fixed_attrs'].to_json %>;
var attrs = <%= raw APP_CONFIG['buyer_attrs'].select {|k, v| @buyer_attrs.include?(k) }.to_json %>;
</script>

<%= render "flow" %>
<h2>行业: <%= @industry.name %></h2>
<p>下面是所选择的参数的直方图，通过拖拽灰色区域可以控制参数的范围以便对公司进行筛选</p>
<hr>
<div class="filter row">
</div>
<table id='jq-grid'></table>
<div id="pager"></div>
<button id="get_similar_buyer" class="btn btn-primary" url="<%= total_get_similar_buyers_url %>">获取参考的公司</button>
